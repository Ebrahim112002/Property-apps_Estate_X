import 'package:flutter/material.dart';

class AppColors {
  // Primary colors from your palette
  static const Color primary = Color(0xFF6367FF);
  static const Color secondary = Color(0xFF8494FF);
  static const Color accent = Color(0xFFC9BEFF);
  static const Color background = Color(0xFFFFDBFD);

  // Functional colors
  static const Color surface = Colors.white;
  static const Color textMain = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF757575);
  static const Color error = Color(0xFFE53935);
}
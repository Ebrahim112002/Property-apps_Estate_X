import 'package:flutter/material.dart';

class AppColors {
  // ==================== REAL ESTATE LUXURY PALETTE ====================
  static const Color primary = Color(0xFF0A1931);     // Deep Navy (Main Brand Color)

  static const Color accent = Color(0xFF785D32);      // Warm Gold (Accent)
  static const Color royalblue = Color.fromARGB(0,17,34,80);
    
  static const Color shellstone = Color.fromARGB(0,245,240,233);   // Soft off-white (Background alternative)


  static const Color secondary = Color(0xFF4A7FA7);   // Supporting Blue

  // Backgrounds
  static const Color background = Color(0xFFF8F9FC);  // Light warm gray-white (Recommended)
  // static const Color background = Colors.white;    // Pure white (jodi strictly chai)

  static const Color surface = Color(0xFFFFFFFF);     // Cards, Bottom Nav, etc. er jonno pure white

  // Text
  static const Color textMain = Color(0xFF1C2533);
  static const Color textSecondary = Color(0xFF5F6B7E);

  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF2E7D32);

  // Extra
  static const Color lightGold = Color(0xFFF5E8C7);
}